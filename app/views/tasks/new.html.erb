<h1>タスク新規作成ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit 'タスクを新規作成する' %>
<% end %>

<%= link_to 'タスク一覧ページに戻る', tasks_path %>